// Temporary placeholder slices until we implement them properly

export const skillSlice = { reducer: (state = {}, action: any) => state };
export const matchSlice = { reducer: (state = {}, action: any) => state };
export const sessionSlice = { reducer: (state = {}, action: any) => state };
export const creditSlice = { reducer: (state = {}, action: any) => state };
export const notificationSlice = { reducer: (state = {}, action: any) => state };
export const videoCallSlice = { reducer: (state = {}, action: any) => state };
export const appSettingsSlice = { reducer: (state = {}, action: any) => state };

export default skillSlice.reducer;
