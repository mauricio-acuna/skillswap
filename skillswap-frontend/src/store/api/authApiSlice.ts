// Temporary API slice placeholders
export const authApiSlice = {
  reducerPath: 'authApi',
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};

export const userApiSlice = {
  reducerPath: 'userApi', 
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};

export const skillApiSlice = {
  reducerPath: 'skillApi',
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};

export const matchApiSlice = {
  reducerPath: 'matchApi',
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};

export const sessionApiSlice = {
  reducerPath: 'sessionApi',
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};

export const creditApiSlice = {
  reducerPath: 'creditApi',
  reducer: (state = {}, action: any) => state,
  middleware: (getDefaultMiddleware: any) => getDefaultMiddleware(),
};
